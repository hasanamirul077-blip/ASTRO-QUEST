{
  "name": "threejs-journey-exercise",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build && cp -r src/sound dist/ && cp -r src/asteroids dist/ && cp -r src/images dist/"
  },
  "devDependencies": {
    "vite": "^4.5.0"
  },
  "dependencies": {
    "dat.gui": "^0.7.9",
    "postprocessing": "^7.0.0-alpha-1",
    "three": "^0.160.0"
  }
}
